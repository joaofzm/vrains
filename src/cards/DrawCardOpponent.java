package cards;

import board.DuelFrame;
import board.HandControl;

public class DrawCardOpponent {

	public static void drawCardOpponent() {
		if (HandControl.opponentHand1Occupied==false) {
      	  HandControl.opponentHand1Occupied=true;
      	  DuelFrame.opponentHand1.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand1.setVisible(true); 
 	  
        } else if (HandControl.opponentHand2Occupied==false) {
      	  HandControl.opponentHand2Occupied=true;
      	  DuelFrame.opponentHand2.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand2.setVisible(true);  
 	  
        } else if (HandControl.opponentHand3Occupied==false) {
      	  HandControl.opponentHand3Occupied=true;
      	  DuelFrame.opponentHand3.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand3.setVisible(true); 

        } else if (HandControl.opponentHand4Occupied==false) {
      	  HandControl.opponentHand4Occupied=true;
      	  DuelFrame.opponentHand4.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand4.setVisible(true); 

        } else if (HandControl.opponentHand5Occupied==false) {
      	  HandControl.opponentHand5Occupied=true;
      	  DuelFrame.opponentHand5.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand5.setVisible(true);  

        } else if (HandControl.opponentHand6Occupied==false) {
      	  HandControl.opponentHand6Occupied=true;
      	  DuelFrame.opponentHand6.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand6.setVisible(true);  

        } else if (HandControl.opponentHand7Occupied==false) {
      	  HandControl.opponentHand7Occupied=true;
      	  DuelFrame.opponentHand7.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand7.setVisible(true);  

        } else if (HandControl.opponentHand8Occupied==false) {
      	  HandControl.opponentHand8Occupied=true;
      	  DuelFrame.opponentHand8.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand8.setVisible(true);  

        } else if (HandControl.opponentHand9Occupied==false) {
      	  HandControl.opponentHand9Occupied=true;
      	  DuelFrame.opponentHand9.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand9.setVisible(true);  

        } else if (HandControl.opponentHand10Occupied==false) {
      	  HandControl.opponentHand10Occupied=true;
      	  DuelFrame.opponentHand10.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand10.setVisible(true);  

        } else if (HandControl.opponentHand11Occupied==false) {
      	  HandControl.opponentHand11Occupied=true;
      	  DuelFrame.opponentHand11.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand11.setVisible(true);  

        } else if (HandControl.opponentHand12Occupied==false) {
      	  HandControl.opponentHand12Occupied=true;
      	  DuelFrame.opponentHand12.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand12.setVisible(true);  

        } else if (HandControl.opponentHand13Occupied==false) {
      	  HandControl.opponentHand13Occupied=true;
      	  DuelFrame.opponentHand13.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand13.setVisible(true);  

        } else if (HandControl.opponentHand14Occupied==false) {
      	  HandControl.opponentHand14Occupied=true;
      	  DuelFrame.opponentHand14.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand14.setVisible(true);  

        } else if (HandControl.opponentHand15Occupied==false) {
      	  HandControl.opponentHand15Occupied=true;
      	  DuelFrame.opponentHand15.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand15.setVisible(true);  

        } else if (HandControl.opponentHand16Occupied==false) {
      	  HandControl.opponentHand16Occupied=true;
      	  DuelFrame.opponentHand16.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand16.setVisible(true);  

        } else if (HandControl.opponentHand17Occupied==false) {
      	  HandControl.opponentHand17Occupied=true;
      	  DuelFrame.opponentHand17.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand17.setVisible(true);  

        } else if (HandControl.opponentHand18Occupied==false) {
      	  HandControl.opponentHand18Occupied=true;
      	  DuelFrame.opponentHand18.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand18.setVisible(true);  

        } else if (HandControl.opponentHand19Occupied==false) {
      	  HandControl.opponentHand19Occupied=true;
      	  DuelFrame.opponentHand19.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand19.setVisible(true);  

        } else if (HandControl.opponentHand20Occupied==false) {
      	  HandControl.opponentHand20Occupied=true;
      	  DuelFrame.opponentHand20.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand20.setVisible(true);  

        } else if (HandControl.opponentHand21Occupied==false) {
      	  HandControl.opponentHand21Occupied=true;
      	  DuelFrame.opponentHand21.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand21.setVisible(true);  

        } else if (HandControl.opponentHand22Occupied==false) {
      	  HandControl.opponentHand22Occupied=true;
      	  DuelFrame.opponentHand22.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand22.setVisible(true);  

        } else if (HandControl.opponentHand23Occupied==false) {
      	  HandControl.opponentHand23Occupied=true;
      	  DuelFrame.opponentHand23.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand23.setVisible(true);  

        } else if (HandControl.opponentHand24Occupied==false) {
      	  HandControl.opponentHand24Occupied=true;
      	  DuelFrame.opponentHand24.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand24.setVisible(true);  

        } else if (HandControl.opponentHand25Occupied==false) {
      	  HandControl.opponentHand25Occupied=true;
      	  DuelFrame.opponentHand25.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand25.setVisible(true);  

        } else if (HandControl.opponentHand26Occupied==false) {
      	  HandControl.opponentHand26Occupied=true;
      	  DuelFrame.opponentHand26.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand26.setVisible(true);  

        } else if (HandControl.opponentHand27Occupied==false) {
      	  HandControl.opponentHand27Occupied=true;
      	  DuelFrame.opponentHand27.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand27.setVisible(true);  

        } else if (HandControl.opponentHand28Occupied==false) {
      	  HandControl.opponentHand28Occupied=true;
      	  DuelFrame.opponentHand28.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand28.setVisible(true);  

        } else if (HandControl.opponentHand29Occupied==false) {
      	  HandControl.opponentHand29Occupied=true;
      	  DuelFrame.opponentHand29.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand29.setVisible(true);  

        } else if (HandControl.opponentHand30Occupied==false) {
      	  HandControl.opponentHand30Occupied=true;
      	  DuelFrame.opponentHand30.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand30.setVisible(true);  

        } else if (HandControl.opponentHand31Occupied==false) {
      	  HandControl.opponentHand31Occupied=true;
      	  DuelFrame.opponentHand31.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand31.setVisible(true);  

        } else if (HandControl.opponentHand32Occupied==false) {
      	  HandControl.opponentHand32Occupied=true;
      	  DuelFrame.opponentHand32.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand32.setVisible(true);  

        } else if (HandControl.opponentHand33Occupied==false) {
      	  HandControl.opponentHand33Occupied=true;
      	  DuelFrame.opponentHand33.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand33.setVisible(true);  

        } else if (HandControl.opponentHand34Occupied==false) {
      	  HandControl.opponentHand34Occupied=true;
      	  DuelFrame.opponentHand34.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand34.setVisible(true);  

        } else if (HandControl.opponentHand35Occupied==false) {
      	  HandControl.opponentHand35Occupied=true;
      	  DuelFrame.opponentHand35.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand35.setVisible(true);  

        } else if (HandControl.opponentHand36Occupied==false) {
      	  HandControl.opponentHand36Occupied=true;
      	  DuelFrame.opponentHand36.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand36.setVisible(true);  

        } else if (HandControl.opponentHand37Occupied==false) {
      	  HandControl.opponentHand37Occupied=true;
      	  DuelFrame.opponentHand37.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand37.setVisible(true);  

        } else if (HandControl.opponentHand38Occupied==false) {
      	  HandControl.opponentHand38Occupied=true;
      	  DuelFrame.opponentHand38.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand38.setVisible(true);  

        } else if (HandControl.opponentHand39Occupied==false) {
      	  HandControl.opponentHand39Occupied=true;
      	  DuelFrame.opponentHand39.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand39.setVisible(true);  

        } else if (HandControl.opponentHand40Occupied==false) {
      	  HandControl.opponentHand40Occupied=true;
      	  DuelFrame.opponentHand40.setIcon(SmallCards.minicover);        	  
      	  DuelFrame.opponentHand40.setVisible(true);  

        }
		
	}
}
